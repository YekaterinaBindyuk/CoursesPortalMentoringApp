import { FreshCoursesDirective } from './fresh-courses.directive';

describe('FreshCoursesDirective', () => {
  it('should create an instance', () => {
    const directive = new FreshCoursesDirective(null);
    expect(directive).toBeTruthy();
  });
});
